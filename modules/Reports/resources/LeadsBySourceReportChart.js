window.LeadsBySourceReportChart = class extends CustomChartWidget {

    drawChart (placeholderId, width, height) {
        // TODO: draw the chart using this.chartData
        console.log(placeholderId);
        console.log(this.chartData);
    }
};